{
  "name": "dokublocks",
  "version": "1.0.0",
  "description": "Javascript Game a combination of tetris and sudoku board",
  "scripts": {
    "dev": "nuxt dev --host localhost --port 3000",
    "build": "nuxt build",
    "start": "nuxt preview --host localhost --port 3000",
    "prod": "nuxt build && nuxt preview --host localhost --port 3000",
    "migrate": "node scripts/migrate.mjs",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/theredone89/dokublocks.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/theredone89/dokublocks/issues"
  },
  "homepage": "https://github.com/theredone89/dokublocks#readme",
  "dependencies": {
    "@netlify/neon": "^0.1.2",
    "@vite-pwa/nuxt": "^1.1.1",
    "nuxt": "^4.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.2.5"
  }
}
